# 04-disk-usage-alert

Script to check available disk space in each mounted drive and generate an alert when usage exceeds 80%.

## 📌 Usage
1. Make the script executable:
   ```bash
   chmod +x check_disk_usage.sh
2. Run the script:
./check_disk_usage.sh
