Script to automate software installation on Linux. Reads package names from a file.

## 📌 Usage
1. Add package names to `packages.txt` (one per line).  
2. Make script executable:
   ```bash
   chmod +x software_install.sh
3. Run script:
./software_install.sh
4. Check logs in software_install.log.
